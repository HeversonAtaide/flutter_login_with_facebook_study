import 'package:flutter/material.dart';

abstract class ColorsCustom {
  ColorsCustom._();

  static const backgroundBlack = Color(0xFF16191D);
  static const white = Color(0xFFFFFFFF);

  // Apenas um exemplo para gradiente
  static const gradientRed = [Color(0xFFD32F2F), Color(0xFF9A0007)];
}
